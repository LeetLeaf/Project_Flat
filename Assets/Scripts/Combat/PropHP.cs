using UnityEngine;
using System.Collections;

public class PropHP : MonoBehaviour
{
    public float HP;
	// Update is called once per frame
	void Update () 
    {
        if (HP <= 0)
        {
            
        }
	}
}
